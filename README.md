# CIS25Midterm-
  In the program Part 1: A function isPrime(), I used the bool data type to determine the truth of a condition. When the input number is less than or equal to 1, the number is not prime and returns false. When the input number is 2, it returns true because 2 is the only even prime number. When the input number is greater than 2, a for loop is used to check whether the number can only be divided by 1 or itself. If so, return true, otherwise return false.  
  In the program Part 1: Calculate the area of ​​a rectangle, I used float to declare the area, length, and width of the rectangle, ensured that the input number can be a floating point literal, and then added the rectangle area formula in the code for calculation.  
  In the program Part 2: The array, I used "int arr[10]" to Create an integer array of size 10, and used "i" to represent the input number. Use int to declare and initialize the input variable, then use a while loop to allow the user to continuously enter 10 numbers, and then use a for loop to print the numbers in the array.  
  In the program of Part2: Loop, I used a for loop to print out all even numbers between 1 and 50. I declared and initialized the variable "i" to 0 (int i=0). After each loop, "i" was incremented by 2. After 24 loops, I got all even numbers between 1 and 50.  
  In the program of Part3: Variables, I Declare and initialize variables of different data types (int, float, char, and string), assigned values ​​to them respectively, and then printed them out.  
  In the program of Part3: CIN, I used string name, int age, and int number to determine the data type, then used "cin" to let the user enter data, and finally used "cout" to print out the user's input data.  
  In the program of Part4: A leap year, we need to determine the formula for calculating leap years (numbers that are divisible by 4 but not by 100, or divisible by 400). When the year entered meets this condition, it is a leap year (), so logical operators (&& and ||) are needed to compare the conditions. Then use "ifelse" to judge the condition. When the condition is true, print "This year is a leap year", and when the condition is false, print "This year is not a leap year".  
  In the program of Part 4: Logical Operators, I used "if else" and nested if statements to compare the size of the three integers entered by the user. The program first declares the data type as "int", and then uses relational operators to compare the numbers.  
