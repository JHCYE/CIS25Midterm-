# CIS25Midterm-
In the first part: the program for calculating prime numbers, I used the bool data type to determine the truth of the condition. When the input number is less than or equal to 1, the number is not prime and returns false. When the input number is 2, return true because 2 is the only even prime number. When the input number is greater than 2, use a for loop to check whether the number can only be divided by 1 or itself. If so, return true, otherwise return false./n
In the first part: the program for calculating the area of ​​a rectangle, I used float to declare the area, length, and width of the rectangle, ensure that the input number can be a floating point literal, and then add the rectangle area formula in the code for calculation.
